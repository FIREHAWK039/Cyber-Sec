`whoami`: what are you logged in as
`cd`: where am i
`dir`: what is around me
`dir /a`: shows hidden files
`hostname`: what is name of this computer
``systeminfo``: what is is version of the machine/windows

`dir /s task_brief.txt`:  The `/s` flag tells Windows to search **all subfolders** starting from your current directory and show you the full path if the file exists

 `type task_brief.txt`: read the file we use type in windows cli